# Format = ["STORE_EXCEPTION_AS", NameReg]
# A = NameReg (destination register that will hold the active exception object).
# Binds the currently active exception to the given register (i.e. "except E as e").
Registers[A] = ActiveException
